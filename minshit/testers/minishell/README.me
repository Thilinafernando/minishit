replace the value of the variable 'prompt' with your's shell prompt.
NOTE: the test will make a diff with the output of your shell with the output of BASH
	(so even 'Command not found' and 'command not found' will strike out)

NOTE: do not insert syntax errors in "testilist"
